📁 EHB-AI-Agent-Phase-1.zip

🧠 Phase 1: Natural Language → ZIP Module Builder

📂 Features:
- User inputs text prompt (e.g., "build login for GoSellr")
- System auto-creates folder & README
- Metadata saved in flow folder

⚙️ Setup:
.env:
MONGO_URI=
JWT_SECRET=

Terminal:
cd backend && npm install && node index.js
cd frontend && npm install && npm run dev

✅ Stack: Next.js, MongoDB, Express, JWT, Axios
