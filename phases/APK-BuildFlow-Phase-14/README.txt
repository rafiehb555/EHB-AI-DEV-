📁 APK-BuildFlow-Phase-14.zip

📲 Phase 14: Mobile APK Build Setup (React Native + Expo)

📦 Features:
- React Native + Expo Go setup
- JWT login system
- Screens: Login, Dashboard, Agent
- `app.json` & `eas.json` included for build
- Ready to run `eas build -p android`

📂 Folders:
- mobile/App.js
- mobile/screens/
- mobile/api/
- mobile/app.json
- mobile/eas.json

🛠 Setup:
1. Install Expo CLI
2. Run: `npm install`
3. Login to Expo: `npx expo login`
4. Build: `npx eas build -p android`
